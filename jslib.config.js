/**
 * jslib.config.js
 */

module.exports = {
  libraryName: 'testlib', // TODO: This must be edited for your project.
  entry: './src/index.js',
  bundleSuffix: 'bundle', // this generates `testlib.bundle.js` via npm run build
};

